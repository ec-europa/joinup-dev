joinup_user.unsubscribe_all:
  path: '/user/{user}/unsubscribe-all'
  defaults:
    _form: '\Drupal\joinup_user\Form\UnsubscribeFromAllGroupsForm'
    _title: 'Unsubscribe from all groups'
  requirements:
    _custom_access: '\Drupal\joinup_user\Form\UnsubscribeFromAllGroupsForm::access'
  options:
    parameters:
      user:
        type: entity:user
