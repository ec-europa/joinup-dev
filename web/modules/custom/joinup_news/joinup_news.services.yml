services:
  joinup_news.fulfillment_guard:
    class: Drupal\joinup_news\Guard\JoinupNewsFulfillmentGuard
    tags:
      - { name: state_machine.guard, group: news }
  joinup_news.relations_manager:
    class: Drupal\joinup_news\JoinupNewsRelationManager
    arguments: ['@og.membership_manager']
