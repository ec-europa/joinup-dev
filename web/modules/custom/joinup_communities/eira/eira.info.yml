name: EIRA
type: module
description: 'Customizations for the EIRA community.'
core: 8.x
package: 'Joinup communities'

dependencies:
  - 'filter:filter'

config_devel:
  install:
    - filter.format.eira_format
