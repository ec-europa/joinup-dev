name: Joinup RSS
type: module
description: Exposes RSS feeds in Joinup.
core_version_requirement: ^8.9 || ^9
package: Joinup

dependencies:
  - drupal:views
  - joinup:joinup_search
  - smart_trim:smart_trim
