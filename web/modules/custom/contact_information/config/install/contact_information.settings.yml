# The allowed conditions for the contact information workflow transitions.
transitions:
  validate:
    __new__:
      - authenticated
      - moderator
    deletion_request:
      - moderator
  request_changes:
    validated:
      - moderator
  approve_changes:
    needs_update:
      - moderator
  request_deletion:
    validated:
      - authenticated
