langcode: en
status: true
dependencies:
  config:
    - rdf_entity.rdfentity.collection
id: rdf_entity-collection-administrator
label: 'Collection owner'
weight: 2
# In Joinup the collection owner is not a typical 'group administrator', i.e.
# they do not have the full set of permissions, so the `is_admin` flag is
# disabled.
is_admin: null
group_type: rdf_entity
group_bundle: collection
group_id: null
permissions:
  - 'request collection deletion'
role_type: null
