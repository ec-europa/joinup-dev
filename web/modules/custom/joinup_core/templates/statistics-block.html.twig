{#
/**
 * @file
 * Default theme implementation to display statistics about content on the site.
 *
 * Available variables:
 * - collection_count: the number of validated collections on the site.
 * - solution_count: the number of validated solutions on the site.
 * - content_count: the number of validated news articles, discussions and
 *   events on the site.
 *
 * @ingroup themeable
 */
#}
<div class="featured-lg__stats">
  <div class="stats__wrapper stats__wrapper__collections">
    <a href="{{ path('view.collections.page_1') }}">
      <div class="stats__icon icon icon--collections"></div>
      <div class="stats__text">
        <div class="stats__text--upper">{{ collection_count }}</div>
        <div class="stats__text--lower">{{ 'Collections'|t }}</div>
      </div>
    </a>
    <div class="stats__description">{{ 'Collaborate around thematics'|t }}</div>
  </div>
  <div class="stats__wrapper stats__wrapper__solutions">
    <a href="{{ path('view.solutions.page_1') }}">
      <div class="stats__icon icon icon--solutions"></div>
      <div class="stats__text">
        <div class="stats__text--upper">{{ solution_count }}</div>
        <div class="stats__text--lower">{{ 'Solutions'|t }}</div>
      </div>
    </a>
    <div class="stats__description">{{ 'Interoperability solutions ready to use'|t }}</div>
  </div>
  <div class="stats__wrapper stats__wrapper__content">
    <a href="{{ path('view.content.page_1') }}">
      <div class="stats__icon icon icon--content"></div>
      <div class="stats__text">
        <div class="stats__text--upper">{{ content_count }}</div>
        <div class="stats__text--lower">{{ 'Content'|t }}</div>
      </div>
    </a>
    <div class="stats__description">{{ 'Events, discussions and news'|t }}</div>
  </div>
</div>
