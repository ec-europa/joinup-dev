{#
/**
 * @file
 * Default theme implementation for a map provided by Webtools Maps.
 *
 * Available variables:
 * - zoom_level: The amount the map is zoomed in when first displayed.
 * - longitude: The longitude for the location shown in the center of the map.
 * - latitude: The latitude for the location shown in the center of the map.
 *
 * @see template_preprocess_oe_webtools_maps_map()
 *
 * @ingroup themeable
 */
#}
<script type="application/json">{
    "service": "map",
    "version": "2.0",
    "render": true,
    "map": {
        "zoom": {{ zoom_level }},
        "center": [{{ latitude }}, {{ longitude }}]
    }
}</script>
