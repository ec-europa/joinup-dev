{#
/**
 * @file
  * Theme override to display a user search result.
 */
#}
{% block content %}
  {{ pattern('card', {
    'variants': 'horizontal',
    'url':  path('entity.user.canonical', {'user': user.id}),
    'title': full_name,
    'date': published,
  }) }}
{% endblock %}
