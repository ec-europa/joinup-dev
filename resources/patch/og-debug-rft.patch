diff --git a/src/OgDeleteOrphansBase.php b/src/OgDeleteOrphansBase.php
index 64adc409..40d011c6 100644
--- a/src/OgDeleteOrphansBase.php
+++ b/src/OgDeleteOrphansBase.php
@@ -86,6 +86,18 @@ abstract class OgDeleteOrphansBase extends PluginBase implements OgDeleteOrphans
           'type' => $entity_type,
           'id' => $orphan,
         ]);
+
+        if ($entity_type === 'og_membership' && $orphan == 172) {
+          /** @var OgMembershipInterface $orphan */
+          $orphaned_entity = \Drupal::entityTypeManager()->getStorage($entity_type)->load($orphan);
+          $group = $orphaned_entity->getGroup();
+          $user = $orphaned_entity->getOwner();
+          $roles = $orphaned_entity->getRolesIds();
+          var_dump($group->label());
+          var_dump($user->label());
+          var_dump($roles);
+          ob_flush();
+        }
       }
     }
   }
